# Per-project runtime UI state (layout geometry, Roadmap 0036)
.ike/

# Explorer session trash (deleted entries kept for undo, fileops.go)
.ike-trash/

# Editor / IDE metadata
.idea/
*.iml

# Built binaries (make / go build)
/ike
/docgen
/shotgen

# mkdocs build output (make docs-deploy / mkdocs build, gh-pages branch)
/site
.issue-work/
