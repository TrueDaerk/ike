# Getting started

Installation, terminal setup, opening your first project, and the first
handful of keys.

!!! info "This section is being written"
    The scaffold for this site landed first so pages can be added one at a
    time. Progress is tracked in
    [Epic 0440](https://github.com/TrueDaerk/ike/issues/1182).

Until the full pages land, the [home page](../index.md) covers installation
and the command-line targets, and the
[README](https://github.com/TrueDaerk/ike/blob/main/README.md) carries the
per-platform notes and the Ghostty configuration example.

Planned pages:

- **Installation** — building from source, where to put the binary, per-platform notes.
- **Terminal setup** — the Kitty keyboard protocol, freeing up chords your terminal claims, verifying with `keyprobe`.
- **Your first project** — opening a directory, the explorer, opening files, saving.
- **Command line** — opening files at a line, multiple tabs, piping stdin.
- **Optional tools** — `ripgrep` and the language servers, and what you lose without them.
